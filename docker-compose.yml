version: '3.1'
services:
  database:
    image: mongo
    ports:
      - 32769:27017
  database-viewer:
    image: mongo-express
    ports:
      - 8081:8081
    depends_on:
      - database
    environment:
      - ME_CONFIG_MONGODB_SERVER=database
