{
  "name": "@zthun/works.proxy",
  "version": "1.0.0-9",
  "private": true,
  "description": "The reverse proxy layer which sits between the client and server for routing.",
  "author": "Anthony Bonta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zthun/works"
  },
  "scripts": {
    "docker:publish": "docker push zthun/works.server",
    "docker:build": "docker build . -t zthun/works.proxy:latest -t zthun/works.proxy:$(node -p \"require('./package.json').version\")"
  }
}
