/**
 * Represents a component that can have a loading flag.
 */
export interface IZComponentLoading {
  /**
   * True if the component is loading.  False otherwise.
   *
   * @default false
   */
  loading?: boolean;
}
