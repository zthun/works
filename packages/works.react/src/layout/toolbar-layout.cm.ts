import { ZCircusComponentModel } from '@zthun/works.cirque';

/**
 * Represents the component model for the ZToolbarLayout component.
 */
export class ZToolbarLayoutComponentModel extends ZCircusComponentModel {
  public static readonly Selector = '.ZToolbarLayout-root';
}
