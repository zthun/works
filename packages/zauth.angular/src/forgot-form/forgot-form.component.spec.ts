import { ZForgotFormComponent } from './forgot-form.component';

describe('ZForgotFormComponent', () => {
  function createTestTarget() {
    return new ZForgotFormComponent();
  }

  it('should create.', () => {
    // Arrange
    // Act
    const target = createTestTarget();
    // Assert
    expect(target).toBeTruthy();
  });
});
