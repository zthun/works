import { ZProfileTopbarComponent } from './profile-topbar.component';

describe('ZProfileTopbarComponent', () => {
  function createTestTarget() {
    return new ZProfileTopbarComponent();
  }

  it('should create.', () => {
    // Arrange
    // Act
    const target = createTestTarget();
    // Assert
    expect(target).toBeTruthy();
  });
});
