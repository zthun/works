/**
 * Injection token for the auth database (IZDatabase)
 */
export const DatabaseToken = 'AuthDatabase';

/**
 * Injection token for the auth model token (OauthModel)
 */
export const OAuthModelToken = 'OAuthModel';
